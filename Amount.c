#include<stdio.h>
int main(){
    char amount,principle,rate,number,time;
    prinf("Principle");
        prinf("rate");
            prinf( "number" );
                prinf("time");
       amount = principle(1+rate/number)^time);
        printf("Amount":%c\n,A);
        return 0;
    }
 